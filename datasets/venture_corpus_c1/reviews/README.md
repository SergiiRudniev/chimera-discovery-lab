# Optional human reviews

This directory preserves the original human-review workflow for optional audits.
It is not the active dataset release gate.

The active policy is `../ai_review_protocol.yaml`; `chimera validate-review-gate`
validates the configured AI review artifacts and snapshot hashes.
