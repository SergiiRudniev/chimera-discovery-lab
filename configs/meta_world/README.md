# Meta-World Configurations

This directory is reserved for Chimera Meta-World configurations. The first
registered model configuration will be `meta_world_w0.yaml` after its tensor,
dataset and qualification contracts are frozen.

No configuration in this directory may reuse Venture graph semantics implicitly.
